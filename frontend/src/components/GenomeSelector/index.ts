/**
 * GenomeSelector component exports
 * Main entry point for the GenomeSelector module
 */

export { default as GenomeSelector } from './GenomeSelector'

// Export types for external use
export type {
    GenomeSelectorProps,
    SpeciesInfo,
    AssemblyInfo,
    AssemblyOption,
} from './types'
